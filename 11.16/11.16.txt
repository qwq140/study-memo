가비지 컬렉션 : 더 이상 참조하지 않을 때 가비지 컬렉션이 발동한다.

1.객체 생성시에 초기화를 위한 생성자를 이용해서 값을 입력한다.
 - 생성자 : 초기화

2.오버로딩 : 하나의 함수이름에 여러가지 기능을 적재
 - 함수의 이름을 통일할 수 있다.
 - 매개변수의 개수, 혹은 타입의 성질이 다르면 다른 함수로 인식한다.
 - 경우의 수가 많으면 사용 불가능
 - 잘 사용된 사례 : println() 함수가 있다.

3. this
 - this는 heap 변수
 - this는 new가 된 heap공간을 Binding 한다.

getter : 값 확인, setter : 값 변경




ctrl + shift + s : 전체 파일 저장
ctrl + shift + f : 자동 정렬
alt + <- : 이전 파일로 이동

alt + shift + s : 생성자 만들때 사용


lombok

빌드패스
-classpath:

파일명 바꾸기 : f2

ctrl + shift + o ( ctrl + space 자주 쓰기 )

import : 내 폴더에 없는 것을 사용